using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scriptis : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("HI, PIDR");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
